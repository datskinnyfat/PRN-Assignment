<Window x:Class="DatPhungWPF.AdminDashboard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodels="clr-namespace:DatPhungWPF.ViewModels"
        xmlns:views="clr-namespace:DatPhungWPF.Views"
        Title="Admin Dashboard - FU Mini Hotel System" Height="800" Width="1400"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">
    
    <Window.DataContext>
        <viewmodels:AdminDashboardViewModel />
    </Window.DataContext>
    
    <Window.Resources>
        <!-- Modern Menu Button Style -->
        <Style x:Key="ModernMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="25,18"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ffffff20"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Logout Button Style -->
        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#e74c3c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="25,18"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="15"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c0392b"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Icon Style -->
        <Style x:Key="IconStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Modern Sidebar -->
        <Border Grid.Column="0" Background="#2c3e50">
            <Border.Effect>
                <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Border Grid.Row="0" Background="#34495e" Padding="25,30">
                    <StackPanel>
                        <TextBlock Text="🏨" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                        <TextBlock Text="FU Mini Hotel" FontSize="24" FontWeight="Bold" Foreground="White" 
                                   HorizontalAlignment="Center" Margin="0,0,0,5"/>
                        <TextBlock Text="Admin Dashboard" FontSize="14" Foreground="#bdc3c7" 
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                
                <!-- Menu Items -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15,20">
                    <StackPanel>
                        <Button Command="{Binding NavigateCommand}" CommandParameter="Customers">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernMenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentView}" Value="Customers">
                                            <Setter Property="Background" Value="#3498db"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="👥" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="Customers" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding NavigateCommand}" CommandParameter="Rooms">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernMenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentView}" Value="Rooms">
                                            <Setter Property="Background" Value="#3498db"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🏠" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="Rooms" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding NavigateCommand}" CommandParameter="Bookings">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernMenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentView}" Value="Bookings">
                                            <Setter Property="Background" Value="#3498db"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📅" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="Bookings" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        
                        <Button Command="{Binding NavigateCommand}" CommandParameter="Reports">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource ModernMenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CurrentView}" Value="Reports">
                                            <Setter Property="Background" Value="#3498db"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📊" Style="{StaticResource IconStyle}"/>
                                <TextBlock Text="Reports" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
                
                <!-- Logout Button -->
                <Border Grid.Row="2" Padding="15,20">
                    <Button Content="🚪 Logout" Command="{Binding LogoutCommand}" Style="{StaticResource LogoutButtonStyle}"/>
                </Border>
            </Grid>
        </Border>
        
        <!-- Content Area -->
        <Grid Grid.Column="1" Background="#f8f9fa">
            <Border Background="White" Margin="20" CornerRadius="15">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="5" Opacity="0.1"/>
                </Border.Effect>
                <ContentControl Content="{Binding CurrentViewContent}" Margin="20"/>
            </Border>
        </Grid>
    </Grid>
</Window>