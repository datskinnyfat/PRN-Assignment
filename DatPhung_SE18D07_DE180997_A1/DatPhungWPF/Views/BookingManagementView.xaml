<UserControl x:Class="DatPhungWPF.Views.BookingManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:viewmodels="clr-namespace:DatPhungWPF.ViewModels"
             xmlns:converters="clr-namespace:DatPhungWPF.Converters">
    
    <UserControl.DataContext>
        <viewmodels:BookingViewModel />
    </UserControl.DataContext>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title and Action Buttons -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Grid.Column="0" Text="Booking Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="➕ Add New Booking" Command="{Binding AddCommand}" 
                        Background="#4CAF50" Foreground="White" Padding="15,8" 
                        FontWeight="Bold" BorderThickness="0" Margin="0,0,10,0"/>
                <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" 
                        Background="#2196F3" Foreground="White" Padding="15,8" 
                        FontWeight="Bold" BorderThickness="0"/>
            </StackPanel>
        </Grid>
        
        <!-- Search and Filter Panel -->
        <Border Grid.Row="1" Background="#F5F5F5" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔍 Search:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                    <TextBox Width="250" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                             Margin="0,0,10,0" Padding="8,5" BorderThickness="1" BorderBrush="#CCCCCC"/>
                    <Button Content="Search" Command="{Binding SearchCommand}" Width="80" 
                            Background="#FF9800" Foreground="White" BorderThickness="0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
                    <Button Content="All" Command="{Binding FilterAllCommand}" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Online" Command="{Binding FilterOnlineCommand}" Width="80" Margin="0,0,5,0"/>
                    <Button Content="Offline" Command="{Binding FilterOfflineCommand}" Width="80"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Booking List and Form -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="380"/>
            </Grid.ColumnDefinitions>
            
            <!-- Booking DataGrid -->
            <Border Grid.Column="0" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Margin="0,0,10,0">
                <DataGrid ItemsSource="{Binding Bookings}" 
                          SelectedItem="{Binding SelectedBooking}" 
                          AutoGenerateColumns="False" 
                          IsReadOnly="True"
                          SelectionMode="Single"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowBackground="White"
                          AlternatingRowBackground="#F8F9FA">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding BookingReservationID}" Width="50"/>
                        <DataGridTextColumn Header="Customer" Binding="{Binding Customer.CustomerFullName}" Width="150"/>
                        <DataGridTextColumn Header="Room" Binding="{Binding Room.RoomNumber}" Width="80"/>
                        <DataGridTextColumn Header="Booking Date" Binding="{Binding BookingDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="100"/>
                        <DataGridTextColumn Header="Check-in" Binding="{Binding StartDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="100"/>
                        <DataGridTextColumn Header="Check-out" Binding="{Binding EndDate, StringFormat=\{0:dd/MM/yyyy\}}" Width="100"/>
                        <DataGridTextColumn Header="Duration" Binding="{Binding BookingDuration}" Width="70"/>
                        <DataGridTextColumn Header="Total Price" Binding="{Binding TotalPrice, StringFormat=\{0:C\}}" Width="100"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding BookingType, Converter={StaticResource BookingTypeConverter}}" Width="80"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding BookingStatus, Converter={StaticResource BookingStatusConverter}}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch"/>
            
            <!-- Booking Form -->
            <Border Grid.Column="2" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="8" Padding="15" Background="White">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="{Binding IsEditing, Converter={StaticResource BoolToStringConverter}, ConverterParameter='✏️ Edit Booking;➕ Add New Booking'}" 
                                   FontWeight="Bold" FontSize="18" Margin="0,0,0,15" Foreground="#2196F3"/>
                        
                        <TextBlock Text="👤 Customer:" Margin="0,5,0,5" FontWeight="Bold"/>
                        <ComboBox ItemsSource="{Binding Customers}" 
                                  DisplayMemberPath="CustomerFullName"
                                  SelectedValuePath="CustomerID"
                                  SelectedValue="{Binding NewBooking.CustomerID}"
                                  Margin="0,0,0,10" Padding="8,5" BorderThickness="1" BorderBrush="#CCCCCC"/>
                        
                        <TextBlock Text="🏠 Room:" Margin="0,5,0,5" FontWeight="Bold"/>
                        <ComboBox ItemsSource="{Binding AvailableRooms}" 
                                  SelectedValuePath="RoomID"
                                  SelectedValue="{Binding NewBooking.RoomID}"
                                  Margin="0,0,0,10" Padding="8,5" BorderThickness="1" BorderBrush="#CCCCCC">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock>
                                            <Run Text="{Binding RoomNumber}" FontWeight="Bold"/>
                                            <Run Text=" - "/>
                                            <Run Text="{Binding RoomType.RoomTypeName}"/>
                                            <Run Text=" ("/>
                                            <Run Text="{Binding RoomPricePerDate, StringFormat=\{0:C\}}"/>
                                            <Run Text="/day)"/>
                                        </TextBlock>
                                        <Border Background="#4CAF50" CornerRadius="3" Margin="10,0,0,0" Padding="5,1">
                                            <TextBlock Text="Available" Foreground="White" FontSize="10" FontWeight="Bold"/>
                                        </Border>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        
                        <TextBlock Text="📅 Check-in Date:" Margin="0,5,0,5" FontWeight="Bold"/>
                        <DatePicker SelectedDate="{Binding StartDate}" Margin="0,0,0,10" Padding="8,5"/>
                        
                        <TextBlock Text="📅 Check-out Date:" Margin="0,5,0,5" FontWeight="Bold"/>
                        <DatePicker SelectedDate="{Binding EndDate}" Margin="0,0,0,10" Padding="8,5"/>
                        
                        <Button Content="🔄 Check Availability" Command="{Binding RefreshCommand}" 
                                Background="#FF9800" Foreground="White" Padding="10,8"
                                BorderThickness="0" Margin="0,0,0,15" FontWeight="Bold"/>
                        
                        <TextBlock Text="📋 Booking Type:" Margin="0,5,0,5" FontWeight="Bold"/>
                        <ComboBox SelectedIndex="{Binding NewBooking.BookingType, Converter={StaticResource BookingTypeToIndexConverter}}" 
                                  Margin="0,0,0,10" Padding="8,5" BorderThickness="1" BorderBrush="#CCCCCC">
                            <ComboBoxItem Content="🌐 Online"/>
                            <ComboBoxItem Content="🏢 Offline"/>
                        </ComboBox>
                        
                        <TextBlock Text="📊 Status:" Margin="0,5,0,5" FontWeight="Bold"/>
                        <ComboBox SelectedIndex="{Binding NewBooking.BookingStatus, Converter={StaticResource BookingStatusToIndexConverter}}" 
                                  Margin="0,0,0,10" Padding="8,5" BorderThickness="1" BorderBrush="#CCCCCC">
                            <ComboBoxItem Content="✅ Active"/>
                            <ComboBoxItem Content="✅ Completed"/>
                            <ComboBoxItem Content="❌ Cancelled"/>
                        </ComboBox>
                        
                        <Border Background="#F8F9FA" CornerRadius="5" Padding="10" Margin="0,5,0,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                
                                <TextBlock Grid.Row="0" Grid.Column="0" Text="⏱️ Duration:" Margin="0,0,10,5" FontWeight="Bold"/>
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,5">
                                    <TextBlock Text="{Binding NewBooking.BookingDuration}"/>
                                    <TextBlock Text=" day(s)" Margin="2,0,0,0"/>
                                </StackPanel>
                                
                                <TextBlock Grid.Row="1" Grid.Column="0" Text="💰 Total Price:" Margin="0,0,10,0" FontWeight="Bold"/>
                                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding NewBooking.TotalPrice, StringFormat=\{0:C\}}" 
                                           FontWeight="Bold" Foreground="#E91E63" FontSize="16"/>
                            </Grid>
                        </Border>
                        
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" TextWrapping="Wrap" 
                                   Margin="0,0,0,10" Background="#FFEBEE" Padding="8" 
                                   Visibility="{Binding ErrorMessage, Converter={StaticResource StringVisibilityConverter}}"/>
                        
                        <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                            <Button Content="💾 Save" Command="{Binding SaveCommand}" Width="100" Margin="0,0,10,0"
                                    Background="#4CAF50" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                            <Button Content="❌ Cancel" Command="{Binding CancelCommand}" Width="100" Margin="0,0,10,0"
                                    Background="#9E9E9E" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                            <Button Content="🗑️ Delete" Command="{Binding DeleteCommand}" Width="100"
                                    Background="#F44336" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</UserControl> 